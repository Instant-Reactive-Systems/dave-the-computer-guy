
export type TableEntry = {
    pinName: string,
    pinPosition: string,
    componentId: string,
    pinId: string,
};
    
export type Entry = {
    pinName: string,
    pinPosition: string,
    componentId: number,
    pinId: number,
};

